// صفحة إكمال التسجيل (معاملة) — لا تُفهرس في محركات البحث.
export const metadata = {
  robots: { index: false, follow: false },
};

export default function CompleteLayout({ children }) {
  return children;
}
